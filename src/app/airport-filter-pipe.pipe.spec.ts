import { AirportFilterPipePipe } from './airport-filter-pipe.pipe';

describe('AirportFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AirportFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
